#include "Systems/SoundController.hpp"

// Clear implementation file for CMake building standards
namespace Systems {
    // SoundController singleton instance logic is handled in header via static local.
}